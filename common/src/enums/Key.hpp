#ifndef R_TYPE_KEY_HPP
#define R_TYPE_KEY_HPP

#include <cstdint>

namespace cmn {

    enum class Keys : uint8_t {
        R,
        Space,
        Up,
        Down,
        Left,
        Right,
        Escape,
        None,
    };

}

#endif// R_TYPE_KEY_HPP
