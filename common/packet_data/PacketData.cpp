/*
** EPITECH PROJECT, 2025
** R_Type
** File description:
** PacketData
*/
#include "PacketData.hpp"

namespace server {

    sf::Packet &PacketData::operator<<(sf::Packet &packet, const PacketData &packetData)
    {
        return;
    }

    sf::Packet &PacketData::operator>>(sf::Packet &packet, PacketData &packetData)
    {
        return;
    }

}// namespace server